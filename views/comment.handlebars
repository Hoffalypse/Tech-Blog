
<section class="space">
  <div class="box2">
    <p class="color">  {{review.title}}<span class="end">created by {{review.user.user_name}} on {{format_time review.updatedAt}}</span></p>
    <p>{{review.content}}</p>
  </div>
</section>
<section>
  {{#each comments as |comments|}}
{{>section}}
{{/each}}
</section>
